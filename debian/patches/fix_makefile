Description: Fix makefile
Origin: vendor
Forwarded: not-needed
Last-Update: <2013-01-22>

diff --git a/Makefile b/Makefile
index 1aa9a1b..907ddf2 100644
--- a/Makefile
+++ b/Makefile
@@ -25,8 +25,8 @@ depends: devmarker
 	fi
 
 
-build: depends
-	$(REBAR) compile
+build:
+	$(REBAR) compile -vv
 
 
 etap: test/etap.beam test/util.beam
@@ -34,7 +34,7 @@ etap: test/etap.beam test/util.beam
 
 
 eunit:
-	$(REBAR) eunit skip_deps=true
+	$(REBAR) eunit -vv skip_deps=true
 
 
 check: build etap eunit
